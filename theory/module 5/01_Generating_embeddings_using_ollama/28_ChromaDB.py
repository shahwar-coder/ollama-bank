'''
Chroma DB (Concise Summary)

👉 What is Chroma?
- Lightweight vector database for storing and retrieving embeddings

👉 What does it store?
- Embedding vectors
- Original documents
- Metadata (source, id, tags)

👉 How does it work?
1. Text → embedding
2. Store embedding + document + metadata
3. Query → embedding
4. Similarity search → retrieve relevant documents

👉 When to use?
- Local RAG apps
- Prototyping
- Hackathons
- Simple persistent vector storage

⭐ One-line:
Chroma is a developer-friendly vector database that stores embeddings with documents and enables semantic retrieval for RAG.
'''